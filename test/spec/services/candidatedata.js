'use strict';

describe('Service: candidateData', function () {

  // load the service's module
  beforeEach(module('electionsApp'));

  // instantiate service
  var candidateData;
  beforeEach(inject(function (_candidateData_) {
    candidateData = _candidateData_;
  }));

});
