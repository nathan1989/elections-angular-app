'use strict';

describe('Service: acUrl', function () {

  // load the service's module
  beforeEach(module('electionsApp'));

  // instantiate service
  var acUrl;
  beforeEach(inject(function (_acUrl_) {
    acUrl = _acUrl_;
  }));

});
