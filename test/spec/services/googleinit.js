'use strict';

describe('Service: googleInit', function () {

  // load the service's module
  beforeEach(module('electionsApp'));

  // instantiate service
  var googleInit;
  beforeEach(inject(function (_googleInit_) {
    googleInit = _googleInit_;
  }));

});
